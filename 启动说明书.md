# 🚀 项目启动指南 (Project Startup Guide)

欢迎使用 **River Floating Detection System (河道漂浮物检测系统)**。本文档旨在帮助您从零开始快速搭建和启动项目环境。即使您是初学者，按照以下步骤操作也能轻松完成。

## 📋 目录 (Table of Contents)

1. [环境准备 (一键脚本)](#1-环境准备-一键脚本)
2. [手动环境安装 (如果脚本失败)](#2-手动环境安装)
3. [数据库配置](#3-数据库配置)
4. [启动后端服务](#4-启动后端服务)
5. [启动前端应用](#5-启动前端应用)
6. [启动 AI 推理服务](#6-启动-ai-推理服务)
7. [常见问题 FAQ](#7-常见问题-faq)

---

## 1. 🔍 环境准备 (一键脚本)

我们提供了一个 PowerShell 脚本来帮助您检查并安装所需的依赖环境（Java, Maven, Node.js, Python, MySQL）。

**操作步骤：**

1.  在项目根目录下，找到 `scripts` 文件夹。
2.  右键点击 `setup_env.ps1`，选择 **"使用 PowerShell 运行"**。
3.  脚本会自动检查您的环境，如果缺少组件，会提示您输入 `Y` 进行安装。

> **注意**：安装完成后，建议 **重启电脑** 或 **重启所有命令行窗口** 以确保环境变量生效。

---

## 2. 🛠 手动环境安装

如果自动脚本无法使用，请参考以下链接手动安装必备软件：

| 软件 | 说明 | 推荐版本 | 下载地址 |
| :--- | :--- | :--- | :--- |
| **JDK** | 后端运行环境 | JDK 17 (或 8) | [Adoptium Temurin](https://adoptium.net/) |
| **Maven** | Java 构建工具 | 3.6+ | [Apache Maven](https://maven.apache.org/download.cgi) |
| **Node.js** | 前端运行环境 | LTS (18+) | [Node.js 官网](https://nodejs.org/zh-cn/) |
| **MySQL** | 数据库 | 8.0+ | [MySQL Installer](https://dev.mysql.com/downloads/installer/) |
| **Python** | AI 模型运行环境 | 3.8 - 3.11 | [Python 官网](https://www.python.org/downloads/) |

---

## 3. 🗄️ 数据库配置

在启动项目前，需要初始化数据库。

1.  **启动 MySQL 服务**。
2.  **创建数据库并导入数据**：
    *   推荐使用数据库管理工具 (DBeaver, Navicat, 或 MySQL Workbench)。
    *   连接数据库后，运行 `database/init.sql` 文件中的 SQL 脚本。
    *   **或者** 使用命令行：
        ```bash
        # 登录 MySQL (假设 root 用户)
        mysql -u root -p

        # 在 mysql 命令行中执行
        source /path/to/your/project/database/init.sql;
        ```
3.  **配置后端连接信息**：
    *   打开 `backend/src/main/resources/application.yml`。
    *   确认 `spring.datasource` 下的 `username` (默认为 root) 和 `password` (默认为 root) 与您的本地 MySQL 设置一致。

---

## 4. ☕ 启动后端服务 (Backend)

后端基于 Spring Boot 开发。

1.  打开命令行 (CMD 或 PowerShell)。
2.  进入后端目录：
    ```bash
    cd backend
    ```
3.  下载依赖并启动：
    ```bash
    mvn spring-boot:run
    ```
    *(如果是首次运行，Maven 会下载大量依赖，请耐心等待)*

*   **成功标志**：看到控制台输出 `Started [ApplicationName] in ... seconds`，且不报错。
*   默认端口：`8080`

---

## 5. 🌐 启动前端应用 (Frontend)

前端基于 Vue 3 + Vite 开发。

1.  打开一个新的命令行窗口。
2.  进入前端目录：
    ```bash
    cd frontend
    ```
3.  安装依赖：
    ```bash
    npm install
    ```
    *(推荐使用 `npm config set registry https://registry.npmmirror.com` 加速)*
4.  启动开发服务器：
    ```bash
    npm run dev
    ```

*   **访问地址**：通常为 `http://localhost:5173` (具体看控制台输出)。

---

## 6. 🧠 启动 AI 推理服务 (Inference Service)

如果您需要使用漂浮物检测功能，需要启动 Python 推理服务。

1.  打开一个新的命令行窗口。
2.  进入推理服务目录：
    ```bash
    cd inference_service
    ```
3.  (可选但推荐) 创建并激活虚拟环境：
    ```bash
    python -m venv venv
    .\venv\Scripts\activate
    ```
4.  安装依赖：
    ```bash
    pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
    ```
5.  启动服务：
    ```bash
    python main.py
    ```

---

## 7. ❓ 常见问题 FAQ

**Q: 启动后端时报错 `Public Key Retrieval is not allowed`?**
A: 这是 MySQL 8.0 的连接问题。请确保 `application.yml` 中的 JDBC URL 包含 `&allowPublicKeyRetrieval=true` (已在最新代码中修复)。

**Q: `mvn` 命令找不到？**
A: 请确认已安装 Maven 并添加到了系统环境变量 Path 中。可以通过运行 `scripts/setup_env.ps1` 尝试修复。

**Q: 前端安装依赖很慢？**
A: 请尝试切换 npm 淘宝镜像源：`npm config set registry https://registry.npmmirror.com`。

---
**祝您使用愉快！**
